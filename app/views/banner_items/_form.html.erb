<%= form_with(model: banner_item, local: true) do |form| %>
  <% if banner_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(banner_item.errors.count, "error") %> prohibited this banner_item from being saved:</h2>

      <ul>
      <% banner_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div> 

  <div class="field">
    <%= form.label :image %>
    <%= form.collection_select :image_id, Image.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :product %>
    <%= form.collection_select :product_id, Product.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :banner %>
    <%= form.collection_select :banner_id, Banner.all, :id, :name %>
  </div> 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
