<%= form_for([@banner, banner_item]) do |form| %>
  <% if banner_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(banner_item.errors.count, "error") %> prohibited this banner_item from being saved:</h2>

      <ul>
      <% banner_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag :banner_id, params[:banner_id] %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div> 

  <div class="field">
    <%= form.label :image %>
    <%= form.collection_select :image_id, Image.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :product %>
    <%= form.collection_select :product_id, Product.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
